# 夜莺工程规范

本文件是夜莺主工程的工程治理唯一入口。夜莺B是已经完成的选根阶段及0.8的上游资产来源；当前正式版本工作看 `work/v08/README.md`。如何记录、实验、修改和发布，以本文件为准。

## 1. 基本原则

1. **唯一真源**：同一种事实只能有一个权威文件；报告和发布物由它生成，不反向手改。
2. **事实、判断、决定分离**：测得的数据是事实；对数据的解释是判断；主人确认的方案才是决定。
3. **结果必须可复现**：任何被拿来比较或发布的结果，必须能定位到代码提交、输入资产、配置、随机种子和命令。
4. **修 Bug 必须带回归测试**：只改实现、不留下能重现旧错的测试，视为未修完。
5. **版本隔离**：旧版本只封存，不用新实验覆盖；新版本不得静默继承布局相关补丁。
6. **生成物不可手改**：确需人工裁决时，写入结构化“谕旨/裁决”资产，再由脚本生成。
7. **先验收口径，再跑长任务**：正式退火前必须通过预检门；参数未定、口径未验时只允许小样例和短跑。
8. **不删除历史理由**：决定被推翻时标记 `superseded` 并链接新决定，不改写成“从未发生”。

## 2. 唯一入口与目录

| 内容 | 唯一入口 | 说明 |
|---|---|---|
| 当前版本范围 | `work/v08/README.md` | 继承什么、不继承什么、当前目标 |
| 当前工作日志 | `work/v08/项目日志.md` | 按时间追加，只记本轮发生的事和链接 |
| 决策 | `work/v08/决策记录.yaml` | 使用 `ADR-xxxx`，保存理由与替代方案 |
| Bug/风险 | `work/v08/问题台账.yaml` | 使用 `BUG-xxxx` / `RISK-xxxx` |
| 参数审计 | `work/v08/退火参数审计.md` | 参数语义、实现口径、验收门槛 |
| 实验 | `work/v08/experiments/<实验ID>/manifest.yaml` | 一次运行一个目录，不混写 |
| 正式候选 | `work/v08/candidates/<候选ID>/` | 只接收已通过验收的实验结果 |
| 发布 | `releases/v0.8/` | 只由发布脚本生成；实验阶段不得写入 |

旧版文档是历史材料，不自动代表当前规则。文档若与上述入口冲突，必须登记问题并以当前版本入口为准。

## 3. 编号规则

- `ADR-0001`：架构、产品口径或不可由数据自动决定的裁决。
- `BUG-0001`：实现与既定口径不一致，或同一输入不能稳定得到应有结果。
- `RISK-0001`：尚未证实为 Bug，但足以影响结论的风险。
- `EXP-YYYYMMDD-NN`：一次可复现实验。
- `RUN-YYYYMMDD-HHMM-<seed>`：实验内的一条实际运行。
- `CAND-YYYYMMDD-NN`：经过比较后保留的候选。

编号只增不复用。被否决或废弃的记录保留原编号和状态。

## 4. 决策记录要求

每项决定至少包含：

- `id`、日期、状态；
- 问题是什么；
- 最终决定；
- 决定依据（数据、原理、用户体验）；
- 考虑过的替代方案及未采用原因；
- 影响的资产和代码；
- 验证办法；
- 若被替代，链接新决定。

聊天中的“可以、加、保留、让位”只有写入决策记录并落实到结构化资产后，才算工程上完成。

## 5. Bug 生命周期

状态统一为：`open → diagnosed → fixed → verified → closed`。另可用 `wont_fix`，但必须写理由。

每个 Bug 必须如实记录：

1. **现象**：用户看到了什么，不先夹带根因推测。
2. **最小复现**：最小输入、命令、预期、实际。
3. **首次引入**：能定位则写提交/脚本；不能定位写 `unknown`。
4. **根因**：哪一层出了问题，为什么此前检查没发现。
5. **影响范围**：哪些历史实验、报告、码表可能失效。
6. **修复**：代码或资产如何改变。
7. **回归测试**：测试名称与通过证据。
8. **防复发措施**：预检、断言、类型约束或流程门禁。

发现 Bug 后先登记再修。若它会使历史结论失效，相关实验必须标记 `invalidated`，不能只重跑后覆盖原目录。

## 6. 实验清单（manifest）

每次退火或正式对照实验必须先创建 manifest，至少固定：

- 实验问题与假设；
- Git commit 和工作区是否干净；若不干净，保存 diff 指纹；
- 可执行文件版本或哈希；
- 配置、elements、字频、读音、当量表、键位分布表的路径和 SHA-256；
- 所有目标权重、层级边界、约束；
- 起点类型、起点文件、随机种子、线程数；
- 温度、步数、停止条件；
- 完整命令；
- 预先写定的验收指标，禁止看完结果再改胜负标准；
- 输出目录、开始/结束时间、运行状态；
- 结论及已知限制。

同一实验比较多个候选时，除被研究的变量外，其余输入必须一致。若不一致，必须拆成不同实验。

## 7. 正式退火门禁

只有以下项目全部通过，才允许长跑：

- 配置可解析且最终配置含有预期参数；
- 音码冻结、一简固定、二简/三简竞争、多音字均通过测试；
- 全码重、简码重、候选位置用手工样例验证；
- 分层边界无 off-by-one；累计层与互斥区间报告不混用；
- 手感指标用 `bao` 等最小样例验证方向；
- 全量计算与增量缓存计算一致；
- 旧版布局补丁未混入；
- 输入资产都有哈希；
- 短跑至少能复现一次并产生完整报告。

门禁结果写入实验目录的 `preflight.md`。任一项失败，实验状态只能是 `blocked`，不能标作正式候选。

## 8. 候选比较规范

每轮先固定“硬门槛”，再看综合表现：

1. 正确性和产品边界；
2. 简码重码与三码覆盖；
3. 分层简码手感；
4. 键位热度和小指负担；
5. 全码重码与全码手感；
6. 字词碰撞、五码辅助等兼顾项。

报告必须同时给：绝对值、相对基线变化、退化项和具体坏例。不能只给一个总分，也不能因总分更好而隐藏局部退化。

### 8.1 完整指标报告门禁

- 优化目标可以只关注某几个指标，但候选验收必须主动报告全部适用指标，不能等人工追问后再补查。
- Chai 自动化审计只允许读取带 `schema_version` 的 `metric.json`；`metric.txt` 仅供人读和兼容历史工具，禁止继续用本地化文本正则作为0.8正式口径。
- 每份候选报告至少同时覆盖：分档码长/三码覆盖、全码与简码重码、有效全码重码、分层简码手感、按键与手指热力、字词碰撞（实验含词时）、总分和复杂度。
- 每个指标必须给绝对值，并与固定基线、上一候选和同批候选比较；明显退化必须展开具体贡献字、编码或键对。
- 当前实验不含某类对象时，报告必须明确写 `N/A（本实验未配置）`，不得静默省略。
- 任一适用字段缺失、JSON版本未知、候选与基线口径不同，统一汇总器必须非零退出；该结果只能标记为“局部目标完成，待完整验收”，不得进入正式候选目录。

## 9. 资产与生成链

- 原始外来数据放只读来源目录，并记录来源、许可、下载时间和哈希。
- 清洗后的数据与原始数据分目录；清洗规则由脚本实现。
- 根集、拆分、读音修正、结构性例外属于上游资产。
- 布局、简码所有权、字词让位、无理码属于布局下游资产，不得跨布局静默继承。
- 发布脚本必须从上游资产和选定候选重新生成全部码表、字根图、查码页和练习器。
- 发布前运行一致性校验，证明生成结果与候选布局、裁决资产一致。

### 9.1 禁止临时脚本修改码表

- 凡是会改变码表、候选顺序、简码所有权、读音、拆分、布局、词位或发布物的操作，必须由仓库内有明确文件名的版本化脚本实现。
- 禁止用 `python -c`、PowerShell管道、交互式解释器、临时here-doc、编辑器批量替换等方式直接改正式资产或候选资产。
- 临时命令只允许做只读查询；一旦查询逻辑会影响裁决，也应落成可复用审计脚本并登记在代码目录。
- 修改逻辑与人工裁决必须分离：脚本实现通用规则，YAML/TSV等结构化资产记录具体裁决。不得把新裁决继续硬编码进一次性脚本。
- 所有写入脚本必须显式接收输入和输出路径；默认不得原地覆盖输入。确需原地更新时必须提供备份、`--in-place`显式开关和变更摘要。
- 正式生成器应支持`--check`或等效预演，发布前由验证器证明可从上游唯一真源完全重建。
- 每个写入脚本都要在 `work/v08/code_audit/代码目录.md` 登记功能、使用阶段和写入范围；未登记脚本不得进入正式链。
- 生成物不得人工修补。发现生成物错误，应修改生成器或结构化输入、补回归测试，再完整重生成。

## 10. Git 规范

- 一次提交只处理一种逻辑主题，提交信息说明“改了什么/为何改”。
- 正式长跑前提交或建立可定位快照；不得以无法还原的脏工作区作为唯一依据。
- 大型运行输出不默认入 Git；保留 manifest、摘要、最佳解、关键日志和哈希。
- 不覆盖旧实验目录；重跑使用新实验 ID。
- 发布标签必须指向通过验收的提交。

## 11. 会话交接规范

每次重要工作结束或上下文变长时，更新 `项目日志.md`，只写：

- 已完成及证据；
- 当前进行到哪里；
- 新发现的问题/决定 ID；
- 下一步唯一入口；
- 正在运行的进程、PID、命令和输出目录；
- 不应重复或不应继承的旧工作。

新会话读取顺序固定为：

1. `工程规范.md`
2. `work/v08/README.md`
3. `work/v08/项目日志.md`
4. `work/v08/问题台账.yaml`
5. 当前实验的 `manifest.yaml`
