# 夜莺码

夜莺码是一套“小鹤双拼音码 + 首末字根形码”的音形输入方案。

## 当前状态

- 当前正式研发版本：**0.8**
- 当前任务：保留已审定的字根和拆分，重新优化字根键位布局
- 可用基准：`releases/v0.7/`
- `夜莺B/` 是已结束的选根阶段及 0.8 上游资产来源，不是当前工作入口。

## 新会话固定阅读顺序

1. `工程规范.md`
2. `work/v08/README.md`
3. `work/v08/项目日志.md`
4. `work/v08/问题台账.yaml`
5. `work/v08/决策记录.yaml`
6. 当前实验的 `manifest.yaml`

旧交接文档、旧版 README 和历史实验只用于追溯；与上述入口冲突时，以 0.8 入口为准。

## 目录边界

| 路径 | 定位 | Git 策略 |
|---|---|---|
| `work/v08/` | 0.8 的资产、台账、测试、实验 manifest 和报告 | 应版本化 |
| `work/v08/experiments/*/output-*/` | 可由 manifest 复现的完整运行输出 | 忽略，不删除本地文件 |
| `releases/` | 已发布或封存的版本 | 应版本化，不手改生成物 |
| `夜莺B/` | 选根历史和上游资产 | 保留，新工作不在此立项 |
| `repos/` | Chai 等外部仓库 | 由各自 Git 管理，主仓忽略 |
| 根目录 `output-*` | 早期运行现场 | 忽略，待单独归档审计，不直接删除 |
| `tests/.tmp/` | 测试临时目录 | 忽略 |

## 工作区安全提示

当前工作区包含尚未整理成提交的 0.8 开发成果。在建立可恢复快照前，不要执行 `git reset --hard`、批量删除未跟踪文件或用旧版发布物覆盖当前资产。
