# 脚本设计0069：冠军单字搜狗版加入快符

## 目的

在C19“单字、出简让全”搜狗表上加入既有`D:/nightingale/symbo.txt`快符，不加入词表、
简词、补码、备用码、无理码或谕旨。

## 规则与门禁

- 快符严格使用`code,position=value`声明，不自行改码或改位次；
- 声明位置若已被单字占用则拒绝生成，不静默挤动原候选；
- 未涉及快符的全部单字行逐项保持；
- 快符加入后，原单字的码和候选序号保持不变；
- 输出独立新文件，不覆盖无快符版。

