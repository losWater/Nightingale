# 脚本设计0072：字词码位裁决记录

## 目的

逐项记录正式单字基线与基础词的人工候选顺序裁决。审查期间只写人类可读裁决表，
不修改正式单字表，不覆盖基础综合字词审查表；全部审查完成后再建立独立机器操作层，
先在假综合表验证，再统一实装。

## 规则

- 每条裁决必须记录码位、单字、原候选、目标候选和理由；
- “字让词”必须明确完整候选顺序，不得只写模糊的升降权重；
- 尚未裁决的组不得进入机器操作层；
- 同一码位再次出现时必须先展示既有裁决，防止无意覆盖。
