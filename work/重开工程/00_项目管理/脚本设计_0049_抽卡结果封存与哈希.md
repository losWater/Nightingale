# 脚本设计 0049：抽卡结果封存与哈希

设计0048承诺记录结果哈希，但首次编排manifest只记录了输出目录。新增独立封存器只读
16份完整结果，对每卡`config.yaml/code.txt/metric.json`计算SHA-256并写回manifest，
同时确认返回码0、状态complete、文件恰好存在。它不改变任何布局或指标。

输出`finalization_report.md`；连续运行应得到相同结果哈希。

