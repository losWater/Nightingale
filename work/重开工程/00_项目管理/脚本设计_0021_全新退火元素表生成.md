# 脚本设计 0021：全新退火元素表生成

## 状态

- 设计时间：2026-08-28 02:51 +10:00
- 状态：已实施并通过双跑验收

## 生成结果

- 全新元素表8454条，覆盖8105字；每条元素序列长度均为4。
- 零频条目336；简码元数据0；历史资产读取0。
- 所有声母、韵母、编码首根和编码末根均能在全新基础配置中解析。
- 连续生成两次元素表哈希均为
  `1e6684be59f5f90237fa4206188acebe22600bc6622a422948cc28795a1a8396`。
- 连续生成两次基础配置哈希均为
  `0d5e38da4639a9a434a6d33b4db33ef6b871d3ee47e8bbc984167748e116a256`。
- 咯、哼、谁、都、噢均按Chai词典自然生成；襄省家族形码来自当前正式8105。

## 输入

- 设计0020生成的全新字音频率表。
- 当前正式8105拆分表中的编码首根、编码末根。
- 当前根集及正式Chai名称别名，用于把规范根名解析为Chai元素ID。
- 当前结构分析基线中的`algebra.szm/mzm`及既有声韵元素集合。

禁止读取历史readings、历史8455元素、历史简码及历史附加码。

## 每条元素

`声母元素＋韵母元素＋编码首根元素＋编码末根元素`

- 声母、韵母由当前Chai algebra规则从去调拼音计算；计算时补中性调数字仅用于满足规则锚点，
  不重新引入声调身份。
- 字根字也使用当前8105表已经验收的首末根，不另设字根字重复规则。
- 每个`(汉字, 去调拼音)`恰好输出一条，频率直接来自全新字音表。
- 不写`简码长度`、`排序序号`、无理码或候选顺序。

## 验收

- 输出条数与全新字音表完全一致。
- 所有声韵及形码元素必须存在于新配置的映射中。
- 所有8105字至少一条；`(汉字, 拼音)`不得重复或遗漏。
- 所有元素序列长度恰为4。
- 不同读音即使最终键位相同仍保留独立条目。
- 连续生成两次，元素表及生成清单的核心输出哈希一致。

## 输出

- `04_Chai输入/全新退火元素表_待核验.yaml`
- `04_Chai输入/全新退火基础配置_待核验.yaml`
- `04_Chai输入/全新退火元素表_生成清单.json`

本步骤只形成无简码的基础退火输入，不启动算法。
