# 脚本设计 0018：襄省根名映射反事实

## 状态

- 设计时间：2026-08-28 02:20 +10:00
- 状态：反事实与正式双跑全部通过

## 反事实结果

- 两次均只变化囊、囔、攮、馕、齉五字，候选8105哈希均为
  `f96e0646d6cd25bfaeafba4acdb0cbf0b9b9ed9f6e93a04b32c7bf1d47b33ef4`。
- 两次五字变化明细哈希均为
  `9d5f7aa8165464780492efecc438da3d81280dda5410e4935f5121fbd257db89`。
- 结构同拆候选新增0、消失0、成员变化0；既有字架37条与历史结构27条均正常命中。
- 因此采用正式名称别名资产接线，不使用五条逐字结构补丁。

## 正式验收结果

- 新增正式资产`02_规范拆分/Chai部件名称别名.yaml`，将夜莺`襄省`精确映射到
  Chai`囊下`，并以U+E83C作为身份守卫。
- 两次完整正式生成的8105哈希均为
  `f96e0646d6cd25bfaeafba4acdb0cbf0b9b9ed9f6e93a04b32c7bf1d47b33ef4`，与候选一致。
- 每次字架规则命中37条、历史结构规则命中27条；结构同拆候选保持15组，变化为0。
- 囊、囔、攮、馕、齉均由Chai自然取出襄省根，不使用逐字后处理。
- 重跑历史8105残差审计后，无法解释项由5降至0。

## 已确认语义

- 夜莺根名：`襄省`，编码宿主为衣。
- Chai字库名：`囊下`，字形ID为U+E83C。
- 用户确认：`囊＝囊字头＋襄省`，囔、攮、馕、齉继承该边界。

## 反事实方法

保持根集、人工拆分及全部正式规则不变，只在候选配置中把当前无法解析的映射键`襄省`
替换为Chai真实字形IDU+E83C，并在结果规范化时将该ID显示回夜莺根名`襄省`。

随后让Chai重新分析全部8105字，再依正式顺序应用既有传播式结构规则、字架规则及27字规则。
候选不得覆盖权威8105。

## 验收

1. 候选仍为8105个不重复汉字。
2. 相对正式表必须恰好只变化囊、囔、攮、馕、齉五字。
3. 五字候选拆分必须分别为：
   - `囊字头＋襄省`
   - `口＋囊字头＋襄省`
   - `扌＋囊字头＋襄省`
   - `饣＋囊字头＋襄省`
   - `自＋田＋丌＋囊字头＋襄省`
4. 既有正式规则命中数不变，不得发生守卫失败。
5. 自动检查结构同拆变化。
6. 独立生成两次，候选表与五字变化明细哈希分别一致。

全部通过后，才允许把`襄省→囊下`作为正式的Chai名称别名接入公共解析器。
