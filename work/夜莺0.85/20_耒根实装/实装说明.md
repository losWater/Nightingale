# 耒根实装（P0020）

2026-09-02，采纳群友提议：立耒为主根，键位n（耒原拆“二＋木”，二在n上）。

## 评估结论（实装前）
- 首根位零变码：耕族核心18字+扩展33字，首根二(n)→耒(n)同键。
- 尾位换码仅9字（全部生僻）：耒、诔、誄、洡、䒹、銇、㑍、筙、䋘，末根木(o)→耒(n)。
- 新增重码3处：诔/誄→lwdn撞类（类保持首选）、洡→lwbn撞淚涙、銇→lwgn撞錑。
- 高频换码0，前6000重码增量≈0；简码lwn（耒）、lwd（诔）保留且仍为新全码前缀。
- 耒本身按“整字根用末根键”判例，全码lwnn（原空码）。

## 改动范围
- 根集两份yaml加主根耒；键位布局yaml加映射耒→n。
- 两份拆分真源：相邻“二＋木”并为耒（前邻为冂的門族除外），核心23字、扩展41字；首末根列同步。
- 两张主表9字换码；扩展字表改码并重算候选位。
- 字根练习txt在文件末尾追加耒行（不改既有行序，保练习进度）；字根表、拆分表、拆分查询、编码反查、部件查字、必拆字同步刷新。

备份见同目录`备份/`。
