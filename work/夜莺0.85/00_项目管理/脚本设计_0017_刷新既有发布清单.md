# 脚本设计 0017：刷新既有发布清单

## 目的

在正式发布目录仅增加或更新明确批准的发布物后，重新计算全部输出文件的 SHA-256，不借助临时命令手填清单。

## 输入与输出

- 输入：既有发布目录及其中的`发布清单.json`。
- 保留：版本、状态、上游输入哈希及统计数量。
- 更新：除清单自身外，发布目录全部文件的输出哈希。
- 可选更新：通过显式`键=整数`参数补充本次新增发布物的统计数量。

## 门禁

- 既有清单的版本必须与目录发布版本一致；
- 清单结构、输入哈希和统计数量必须存在；
- 写回后重新读取，逐项验证输出哈希。
