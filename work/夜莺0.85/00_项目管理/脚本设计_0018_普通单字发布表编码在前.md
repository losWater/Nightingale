# 脚本设计 0018：普通单字发布表编码在前

## 目的

正式发布包中的无词普通单字表采用`编码<TAB>字`格式；上游单字接口C继续保持`字<TAB>编码`，避免改变既有流水线接口。

## 输入与输出

- 输入：正式单字接口C的两列`字<TAB>编码`文件。
- 输出：同样行数、同样行序、逐行交换为`编码<TAB>字`的发布文件。

## 门禁

- 每个非空行必须恰好两列；
- 第一列必须是单个汉字，第二列必须是1—4位小写字母编码；
- 输出反读后逐行交换必须与输入完全一致；
- 不修改搜狗版、挂接字词普通版或上游接口C。
