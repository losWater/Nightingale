# 脚本设计 0009：候选具体重码展开

读取退火原始 `code.txt`，按元素表频率顺序展开指定层级的全码与简码碰撞。有效全码重码定义与libchai一致：全码候选序号大于0，且该身份没有一简或二简保护。脚本必须用独立计数与 `metric.json` 的前1500有效全码重码核对；不一致即失败。

输出JSON机器明细与UTF-8 Markdown人工审查表，不修改任何码位。

同系列的字词碰撞审计读取固定目标词码位表，逐项区分“已有简码避开”“词与字同首字可保留”“直接撞车”，并列出词频排名与具体词。
