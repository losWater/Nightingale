# 脚本设计 0014：稀疏搜狗词库版加入快符

## 目的

在“仅写单字、词位留空不压缩”的搜狗词库版上加入既有快符，同时保留全部稀疏候选序号。

## 门禁

- 源表和快符表的 `(码, 候选序号)` 分别不得重复；
- 快符不得覆盖源表任何已有位置；
- 合并后所有源表三元组必须原样存在；
- 允许候选序号不连续，不补词位、不压缩序号；
- 输出文件必须事先不存在。

## 输出

独立的 `_搜狗词库版_含快符.txt`，不覆盖无快符版本。
