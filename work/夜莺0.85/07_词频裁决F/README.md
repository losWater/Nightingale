# 词频裁决F

本目录只记录作用于已验收接口E的字词候选裁决，不修改正式单字接口C或基础词库D。

- 单字最多只让出一个首选位置；
- 同字叠词默认不压过正常单字，除非有非常强的独立词频证据；
- 有简码身份的保留全码仍按C＋D基础规则自动让首词；
- 默认产物为挂接版，人工词序按长串可恢复性与开放式单字组合需求裁决，不只看独立二字词频；
- 生成人工审查清单前必须先限定 `status=直接撞车待审`；`简码已自动让词` 属于E阶段已经解决的结果，严禁再次写入F；
- 每条裁决记录目标码位的完整关键候选顺序；
- 全部审查完成后统一生成G，不边讨论边覆盖E。
