# 夜莺 0.8 项目日志

> **已被0.8.5接管：** EXP-01—14属于早期搜索历史，不是当前产品状态。最新续接入口见主仓 `README.md`、`releases/v0.8.5/README.md` 和 `work/夜莺0.85/00_项目管理/进度表.md`。

本文件按时间追加，提供会话续接入口；详细理由链接到 ADR、Bug 或实验记录，不在这里重复铺陈。

## 2026-08-29：工作区治理与 EXP-14 状态收口

### 已完成

- 建立根目录 `README.md`，固定0.8唯一阅读入口，并区分当前工程、历史资产、外部仓库与可再生运行输出。
- 扩充 `.gitignore`：忽略根目录早期 `output-*`、0.8实验的完整 `output-*` 运行目录和 `tests/.tmp/`；未删除任何本地结果。
- EXP-14 已确认完成 mixed/move-only 各4链，并已产生完整指标报告；manifest 从 `running` 改为 `completed_pending_decision`。
- 从v0.7归档分支切出 `work/v08-snapshot-20260829`，建立主仓0.8可恢复快照 `eec5d6c`。
- `repos/libchai` 引擎补丁已独立提交为 `eaa3d2d`；GNU工具链的10项Rust测试全部通过。

### 当前结论

- EXP-14 中纯移动4/4链保持前300有效全码重为0，混合移动仅2/4链通过；混合移动没有显示稳定的综合优势。
- 按实验的预注册验收规则，下一轮默认继续使用纯移动邻域；这是待主人确认的工程建议，尚未写入 ADR。
- 0.8的当前源码、测试、资产和实验摘要已有可恢复快照；未跟踪的旧运行现场、0.8.5和“重开工程”未经归属审计，仍保留本地且未纳入快照。

### 下一步唯一入口

- 以 EXP-14 的 move-only + guard4 为默认方向设计下一个预注册实验；启动前在manifest固定主仓 `eec5d6c`之后的实际提交和libchai `eaa3d2d`。

## 2026-08-27：0.8 启动与参数审计

### 已确认

- 夜莺B选根阶段已经结束；当前项目身份为夜莺正式新版本0.8，工作入口已提升到主仓 `work/v08/`。
- 0.8 只重排根键，不重新选根或推翻人工拆分。
- 不设飞键；前两码严格为小鹤双拼。
- 允许个别字存在多个输入码，但不能用来虚增主体性能。
- 简码手感优先于全码手感；提高三码覆盖目标，降低全码重码和全码手感的主导程度。
- 0.7 布局相关无理码、简码谕旨和字词让位全部不继承；只保留结构性拆分例外。
- 正式退火暂停，先完成参数与实现口径审计。

### 已完成

- 建立 `继承边界.yaml`。
- 建立 `退火参数审计.md`，列出正式退火硬门槛。
- 审阅 `libchai` 的目标函数、简码生成和缓存实现。
- 建立项目级 `工程规范.md`、决策记录、问题台账和实验模板。
- 建立 `code_audit/`，登记夜莺新增/修改脚本和上游chai补丁，供Claude独立审计。
- 增加“禁止临时脚本修改码表”规范：正式变更必须由版本化脚本和结构化资产产生。

### 新发现

- `RISK-0001`：简码组合当量混入不可优化的双拼内部键对，虽不改变梯度方向，但稀释报告和参数解释。
- `BUG-0001`：分层 fingering 当前未按字频加权，与“常用字手感优先”的口径不符。
- `RISK-0002`：旧交接/README 含已过时规则，新会话可能误继承。
- `RISK-0003`：部分历史运行器会原地修改实验输入，且上游chai补丁仍处于独立仓库脏工作区，复现链需要加固。
- 外部审查开始复核字根归并理由，已建立 `字根复审台账.md`；第一批8项中，尧、恭字底、享字头已有明确裁决，夂并又已经落实，示、象、鬲、定字底4项需要补做比较或澄清字形。
- 收到简单鹤作者对目标优先级的评审：三码率最重要，兼顾简码手感，再尽量避免字词撞码；同时需验证这些参数在chai有限空间中的真实选择力。原图和解读已归档到 `reviews/2026-08-27_简单鹤作者/`。
- 接收Claude第一轮独立代码审计（SHA-256见`code_audit/审计接收与处置_2026-08-27.md`）。确认现有交叉复算、简码审计、多音字频率和随机起点门禁存在系统性缺口；0.8正式退火继续暂停，先修门禁再修引擎。
- BUG-0003独立交叉复算器已完成第一轮重写，修复4项已知语义偏差并通过9项Python测试；真实历史指标暴露出软碰撞不一致。因旧运行可能受BUG-0002缓存污染，当前状态保持`in_progress`，待建立当前Rust/Python共用黄金夹具后再恢复门禁。
- BUG-0003随后定位并修复“无简码回退时不能按code.txt候选序号追加选择键”的隐藏语义；10项单元测试与1项Rust/Python真实资产集成测试全部通过，两端精确得到硬18、软40.5125。该门禁现已恢复，BUG-0003标记`verified`。
- BUG-0006已建立统一读音频率聚合模块：同字同双拼音码求和、不同音码隔离、顺序无关、坏输入失败。三个消费脚本完成迁移，5项单测与两项完整数据审计通过，状态标记`verified`；`rows[0]`主读音问题仍明确留在后续读音排序契约中。
- BUG-0009已验证：旧读音资产发现23个乱序字；新增加载断言、统一主读音入口和禁止原地覆盖的纯重建器，生成`work/v08/assets/readings.json`。规范资产8105字全量通过，“嗯”主读音恢复为`en/157982`；相关20项测试全部通过。
- BUG-0002已验证：libchai的增量变化集合改为在简码输出后采集，并覆盖“全码或简码实际变化”。三字Rust专项测试可捕获旧逻辑漏项；全库Rust测试、release重建和Rust/Python完整资产交叉对账均通过。
- BUG-0001已验证：不改旧`tiers.fingering`语义，新增按字频累计并按层键对总量归一化的`weighted_fingering`。10:1方向测试、旧配置兼容、release构建和新字段完整资产编码均通过；具体分层权重仍须另做参数定标。
- RISK-0001已缓解：新增独立`phonetic_shape_transition_equivalence`，只统计双拼末键到首型键，按频率归一化且不改变旧`pair_equivalence`。`bcx`方向数据、Rust专项测试、release完整资产验收及22项Python回归均通过；正式权重仍待小规模对照定标。
- BUG-0004已验证：最终简码审计器改为按资产行保留同字多码，建立反向所有权索引，并独立复算显式一二简与自动三码。修复过程中补齐配置结构、内部排序和`count`缺省值三项口径；10项专项测试及8455行release完整资产对账通过，所有权差异0，合并回归32项通过。
- BUG-0005已验证：历史前沿汇总器取消正则miss静默跳过，改以manifest核对profile和线程完整性；坏metric、目录异常及零有效候选全部失败即停。7项专项测试通过，历史3×12 suite的36项结果完整复算，合并回归39项通过。该脚本仍只解析历史层级格式，0.8格式会明确阻断而非静默漏项。
- BUG-0007已验证：随机起点生成器改用依赖图和随机化回溯，单选元素也重建，生成后全量验约束；环、未知操作符/引用和无解均失败，seed必填并入配置。三元素链×1000 seed及7项边界测试通过；完整474元素资产seed=777双份哈希一致并由release成功编码，合并回归47项通过。当前正式资产尚无条件候选，真实条件资产仍须在引入时另做验收。
- BUG-0008已验证：保护实验运行器不再修改源目录，强制新output-run且拒绝覆盖；manifest记录命令、状态、时间及六类输入SHA-256，日志排他创建。5项专项测试和历史实验1步真实短跑通过，源配置哈希前后不变，合并回归52项通过。旧保护目标仍属历史，不因运行器安全化而获准进入0.8。
- 正式切回“由Codex代按Chai按钮”的工作流。新增版本化0.8输入构建器：8455单字仅保留26个一简，删除405个0.7显式二简及旧字词/五码目标；自然简码依次竞争二简与三码。EXP-01发现二简规则遗漏并作废，EXP-02验证二简恢复，EXP-03形成正式零权重测量基线：前6000三码3305，独立简码所有权复算8455行差异0。下一步进入单变量短跑，不再逐个现代化无关历史脚本。
- 接收简单鹤作者第二轮手感意见：按键频率不作为主目标，前1500全码重应区分一二简保护，大跨与小跨是主要机器手感项，其余交给长期实打。新增有效全码重码全局/分层字段与保护状态增量刷新；EXP-04完整基线裸重与有效重均为0/1/21/405，前6000三码保持3305，Rust 9项与Python 57项测试通过。

### 当前入口

- 建立“优化可局部、验收必须完整”的强制门禁：Chai命令行现在在保留`metric.txt`之外写出`schema_version=1`的完整`metric.json`，并增加嵌套字段不丢失的Rust回归测试。默认MSVC工具链因本机缺少`link.exe`无法构建，既有GNU工具链专项测试通过；统一汇总器和EXP-05—11追溯验收仍待完成，因此现有v1/v2只保留为局部目标候选。
- 完成统一完整指标汇总器及标准库回归测试：适用字段缺失、未知JSON版本、分档缺失或手感数组不完整均失败即停；报告同时给绝对值、基线差值、可选前任差值、热力和具体异常字组。真实回放又发现`chai encode`未归档输入配置，已修为每次保存`config.yaml`；不同目标函数的总分现在明确禁止横比。
- EXP-11的基线、v1-3794、v2-3797已由新版release重新编码并通过完整字段门禁，统一报告见`experiments/EXP-20260827-11/full_metrics_report.md`。两案前6000三码为3794/3797，但共同把前300全码重从0增至1，具体为v1 `qicy`其/期、v2 `dekl`得/德；v1在前1500三码、小跨和全码重上整体更均衡，当前仍仅为通过数据完整性复验的实验候选，不自动晋级正式候选。
- EXP-12以v1为基准测试前300有效全码重权重2/8，各4链×2万步。完整逐链报告纠正了只看优化器末尾候选的误判：guard2仅2/4链清零，guard8为4/4；但guard8前6000三码中位数3770.5，较guard2的3783.5少13且最低3758。两档形成选择力边界，下一轮测试权重4；固定9项摘要已据逐链数据更正归档。
- EXP-13测试中间权重4，4/4链前300有效全码重清零；前6000三码中位数3786.5（3778—3795），高于guard2和guard8，前1500大/小跨中位数0.594%/8.671%。采用guard4作为后续轻保护参数；热力链间波动仍大，只作候选后筛。

- EXP-05 已完成前6000三码字单变量边界探测：4×20万步结果为3759/3769/3761/3754，全部超过3690参照值；同时证实单一总量目标不能自动保护前300/500与简码跨排手感。该轮只作为可达边界证据，不产生发布候选。下一步进入分层三码与简码大跨/小跨的小规模权重扫描。
- EXP-06完成分层三码＋简码跨排第一轮：4×2万步得到3721—3741个前6000三码字，且前300大跨四链均为0，前300小跨最低7.34%，前1500大/小跨最低组合约0.58%/8.38%。线程2总分最高，线程1在前1500三码与小跨形成另一Pareto点；方向验证通过，但须补具体坏键对贡献审计后再扩步数。
- EXP-07/08测试第三键`z/x`显式热力软约束：权重2.0时三码3657—3661，降至0.5后仍仅3642—3685，均未过3690门槛；同时`f/h`出现被动过度搬运。两档均拒绝，热力改为候选后筛，EXP-06线程2继续作为主候选；三码分层权重始终保持并已由配置断言核验。
- EXP-09把前6000三码加至-75并将大/小跨降至80/15，4×2万步达3772—3789；线程1以3789、前1500三码1016及健康热力成为主候选。EXP-10用旧权重套同温度控制后同样达到3788，说明本轮跃升主要受更低最低温/搜索轨迹推动，两组权重差异尚小；后续应统一温度与起点做重复，不再凭单轮极值过拟合权重。
- EXP-11将v1/v2在同温同起点下各扩至12链：前6000三码中位数3780/3783，前1500大跨0.575%/0.601%，小跨8.569%/8.745%；v2略偏三码，v1略偏手感且热力自然通过率更高。最终保留v1-3794为主候选、v2-3797为偏三码Pareto候选，不再以单次最高3800直接裁决。

下一步从 `退火参数审计.md` 的门槛逐项做最小测试；先处理简码、多音字和分层手感，不启动正式退火。

- 2026-09-02（Claude）ADR-0003：0.8.5 四码普通词改用简单鹤纯词库的内容与词序。新增版本化脚本 `work/夜莺0.85/scripts/reorder_v085_words_from_jdhe.py`（默认预演、`--in-place` 需 `--backup-dir`、输出变更摘要与逐码位明细）与回归测试 `tests/test_reorder_v085_words_from_jdhe.py`。综合主表 172,672→211,007 行（+38,335 条简单鹤二字词，3,050 条飞键/多音词重编到夜莺码位，38,543 个码位顺序有变，3,828 条四码简词候选位更新）；单字、裁决字词、实战修改全部守位（有断言与逐项复核）。随后 `pipelines/v085/rebuild.py` 重建 02 挂接、`build_reverse_lookup.py` 重生成 04 编码反查、`refresh_v085_release_metadata.py` 刷新发布清单，`pipelines/verify.py` 通过。摘要与明细见 `releases/v0.8.5/05_维护与裁决/普通词序采用简单鹤_变更摘要.json` / `_变更明细.tsv`，交接说明见同目录 `普通词序采用简单鹤_交接说明.md`。
