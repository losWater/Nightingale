# 0.8 实验目录

一次可复现实验使用一个独立目录：`EXP-YYYYMMDD-NN/`。

目录至少包含：

- `manifest.yaml`：实验问题、输入哈希、参数、种子、命令和状态；
- `preflight.md`：正式运行门禁结果；
- `runs/`：各次运行输出；
- `report.md`：绝对指标、相对基线、退化项、具体坏例和结论。

不得覆盖旧实验。修改任一输入、权重或验收标准后重跑，必须创建新实验 ID。

