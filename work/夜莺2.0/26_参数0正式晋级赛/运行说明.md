# 参数0正式晋级赛

512次，每次50000步；32组，每组投影、小改、大改、随机各4个。每组4名进入128强，换语料复测后取5名。参数0、最终评分、字频、根集与简码规则冻结。保留前300有效选重为0晋级门槛，缺额不静默放宽。

第一轮沿用原第一批10000句。第二轮新种子抽样10000句，排除原第一轮和原第二轮全部文本，各来源2500句；所有128方案使用相同新语料。新旧第二轮综合分不能不加说明直接横比。原生退火随机流未固定，种子仅控制初始布局及抽样。

小文件及工具在frozen冻结，原始大语料只读引用E盘原15目录，不重复占用磁盘。四并发上限、内存启动阈值6GB、磁盘12GB、异常自动降并发与重试；进度保存status.json。生成普通单字表和完整双模式结果，不修改正式发布。
