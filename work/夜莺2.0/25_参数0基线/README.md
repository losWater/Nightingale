# 参数0：下一阶段基线

用户确认以参数0为起点。这里确认的是退火参数集，不是某一张码表或唯一布局。

- 前3000有效重码保护，分档权重80；全域字频有效重码权重300。
- 手感×1.5：对应既有大跨、小跨和音形过渡非零权重；其他目标保留参数0原值。
- 每次50000步，温度、移动策略及完整目标见退火参数.json。
- 后续继续多起点；正式评分、根集与简码约定沿用冻结输入。
- 原8次验证均分67.75594；仍保留其他参数为历史对照，不代表参数0各指标全面最优。
- 完整母配置使用原投影布局，可供生成后续实验；不使用综合分最高样本锁定布局。
- 本次只确认并落盘基线，没有另启动大规模计算，也未改写历史实验或发布文件。
