# 夜莺B 工作日志（2026-08-24）

> 本文是本轮长会话的阶段性交接入口。继续工作时先读本文，再按需查 `选根台账.md` 的具体裁决。`根集.yaml`、`拆分规则.yaml` 和重建后的 `analysis.tsv` 才是当前实现状态的事实来源。

## 1. 当前目标

夜莺B 正在按音节逐字重选字根，替换夜莺 0.4 中大量由机器自动拆分产生、但人眼不直观的结果。

编码形式为 `abxy`：

- `ab` 是固定的小鹤双拼音码，当前阶段不可调整。
- `x` 是首根，`y` 是末根。
- 中间部件在最终码表中不可见。
- 所以真正的问题是：在一个给定读音下，如何用首根和末根区分同音字。

当前根集：**主根 155，附属／锚定 189，零就业 0**。人工部件拆分 11 条，整字序列覆写 22 条。

## 2. 首末根原理

这是整个项目的核心判断方法，简称“原理”。

若一个字的实际拆分为 `x * y`，其中 `*` 是任意数量的中间部件，那么在码表层只留下 `x` 和 `y`。因此：

1. 候选部件若只改变不可见的中间部分，就不会改变全码。
2. 一个较大的部件与一个更小、更熟悉的首根具有相同首端时，往往只保留小根即可。例如虍可拆卜、厂、匕；在虚等字中间部分不可见，所以不必再立虍。
3. 判断末根必须倒着看。不能因为一个大部件视觉上完整，就默认它应整体成为末根；应先确认从字尾向前第一个可见根是什么。曷最终没有整体挂日，而是把末端 `匃` 挂入框家。
4. 若某字不立根后形成 `abxx`，它在码面上等价于自身成根的打法；若合并进一个直观近形家族且不产生碰撞，通常合并更省记忆。
5. 原理只保证首末码面等价，不代表中间拆分可以随意。人工边界仍要符合人眼结构，并防止以后新增根后意外改变首末链。

## 3. 两项正式评价指标

每个候选必须检查全部常用字、全部读音，不能只检查它最初出现的音节。

### 三码简码 `abx`

- 每个音节先排除唯一的二简字 `ab`。
- 剩余字按首根 `x` 分堆。
- 每堆只有最高频的一个字取得三码 `abx`，其他字使用全码。
- 比较候选实施前后：谁新掉出三码、谁获救、是否只是低频字让位。
- 二简字不参与三码位置竞争，脚本已修正这一点。

### 全码 `abxy`

- 同音节且首末根完全相同才算全码重码。
- 必须区分“原本就撞”与“候选新增”。候选只为增量负责。
- 用户可以接受少量低频／零频全码重码，但高频真实撞车优先否决。

### 多音字规则

一个字有多个读音时，所有读音都参与检查。不能用冷门读音没有重码，掩盖常用读音上的碰撞。

当前频率数据把整字总频复制给每个读音，因此会夸大冷门音（例如省 `xing`、万 `mo`、世 `xie`）。看到多音字改变三码归属时必须人工判断真实读音频率，不能照单全收。

## 4. 标准工作流程

### A. 开一个音节

```powershell
$env:PYTHONUTF8='1'
python .\夜莺B\scripts\syl_table.py di
```

先把该音节的全部常用字、首根堆、末根和结构明细列给主人。不要一上来替主人连续裁决多个字。

### B. 发现候选部件

先确认三件事：

1. `根集.yaml` 中是否早已有这个根或附属，不能凭记忆判断。
2. chai 中整字、偏旁形、省形分别叫什么，是否为不同元素。
3. 它有哪些直接首末用户，是否遗漏对应部首和形近字。

常用命令：

```powershell
rg -n "候选字" .\夜莺B\work\根集.yaml .\夜莺B\work\选根台账.md
python .\夜莺B\scripts\chai_lookup.py 候选字 相关字1 相关字2
```

### C. 跑“有根／无根”全字集双检

独立根或单个候选：

```powershell
python .\夜莺B\scripts\candidate_root_check.py "弟"
```

同键合并／主根加附属：

```powershell
python .\夜莺B\scripts\candidate_root_check.py "氏+氐"
```

脚本没有标准 `--help`；传入 `--help` 会被误当作候选根。不要这样调用。

向主人汇报时按以下顺序：覆盖字族 → 三码新增／获救 → 全码新增／解除 → 直观性与根数代价。数据给完即停，等主人裁决。

### D. 主人裁决后落盘

只有主人明确说“加／合并／不加”后，才能把决定写入正式文件：

- `根集.yaml`：主根、附属、锚定关系。
- `拆分规则.yaml`：chai 自动拆分不能表达的人工边界。
- `选根台账.md`：字族范围、决定、理由、三码和全码增量。

新增一个根时必须再次提醒并检查：对应偏旁、部首形、省形和容易遗漏的形近变体。

### E. 重建与审计

```powershell
python .\夜莺B\scripts\build_analysis.py --run
python .\夜莺B\scripts\audit_rules.py
```

确认根元素数量、附属／锚定数量、零就业数、人工边界状态。审计通过后再进入下一个候选。

### F. 音节收官

重新运行 `syl_table.py 音节`，确认所有常用字均已逐项裁决，把收官状态写进台账或本日志。然后才进入下一个音节。

## 5. 已知工具边界与假报告

`candidate_root_check.py` 当前没有带入所有整字序列覆写，因此每次候选检查常重复出现以下假变化：

- 临
- 羲／曦／爔
- 彖／蠡
- 庸／痈、埇／墉、鲬／鳙等庸家族
- 隶／逮

这些并非候选导致，不能记入新增或解除。正式 `build_analysis.py --run` 会应用序列覆写。

另外，chai 自动结构不是最终裁决：它可能跨借笔画、拆散人工定义根，或把独体字落成笔画。遇到反直观结果先查结构树，再决定附属根、人工部件拆分或整字序列覆写。

## 6. 本轮重要设计成果（摘要）

详细数字见 `选根台账.md`。本轮已经形成的主要家族／边界包括：

- 争字底家：上出头、下出头、两端出头、无竖的彐家及聿等统一同键；秉为独立根并锚定该键。
- 庚字心内部固定拆为争字底+人，并不整体立根；庚序列为广+争字底+人，码面取广—人。
- 氏、民、至、之、隹、甫、弗、付、居、厉、里、丽、莫、黑、暴、牙、乃、弟等成为主根。
- 牛家收牛字旁、制字旁；王家收丰、玉旁、青头；车家收车旁、拣字边，舟与车锚定。
- 月家保留月旁并保护青字底；皿家收罒、四、署、血。
- 框家目前为 `匚:[凵,冂,门,巨,勹,匃]`；曷取日—框，不立完整曷。
- 戈家收弋、戋、戊；武固定拆横、弋、止。
- 弟为最新落盘主根，弟省作附属：第取竹—弟，递取弟—辶。

根数目标是尽量控制在 150 左右、硬上限预期 150 以内曾被提出；目前已到 155，因此后续更应优先合并／附属与按原理不立根，但不能为了数字牺牲明显直观性和真实重码性能。

## 7. 音节进度

已收官：

`yi、ji、yu、ui、vi、fu、qi、xi、yj、jm、ww、li、uu、xm、jp、jk、wu、jn、ju、vu、yz、bc、mo、bi、lu、xp`

已收官至：**di**。

`di` 已确认并收官：

- 也早已是独立主根，地取土—也，不处理。
- 弟成为主根，弟省作附属；已落盘、重建、审计通过。
- 氐挂氏；帝字头挂宀帽子家；古独立成根；商为主根并收啇。

氐已挂入氏家并落盘。全字集结果：三码新增 0、获救 0；全码新增 0；仅解除茋／菂一对零频重码。低、底、抵由末点改末氏，邸码面不变。

帝字头已挂入宀帽子家（原家族为冖、学无子、尚头），不增加主根；全部字音三码、全码均无新增。帝取宀家—巾，旁取宀家—方；帝在蒂／谛等字末端时仍由巾接管。

古已成为独立主根：50个直接首末用户，全部字音三码新增／获救均0、全码新增0，仅解除牯／牿零频碰撞。

随后主人提出把商与啇统一为商根。正式方案为商主根、啇附属；两者外壳相同，仅框内八／十不同。最新基线三码新增／获救均0、全码新增0；商取商—商，滴取氵—商、摘取扌—商、嫡取女—商。古仍保留自身大字族。

当前暂停点：`di` 已收官，下一音节为 `xl`。

`xl` 开始后建立向家：向为主根，附属向字框、囱字框、囱、囟、身。形体口径为“撇起笔的包围框”；奥、粤／粵、窗、身旁家均由此接管。正式双跑三码新增0、获救粤，全码新增0、真实解除0。

乡不独立立根，作为连续折转的丝线形挂入纟／幺家；三码、全码增量均为0。

㐄以项目部件名“降下角”成为独立主根，统一夅／舛共同末端，覆盖降、舞、麟、磷、瞬、鳞、舜等32字／33字音；三码、全码增量均为0。

襄成为独立主根，作为衣字架规则的边界保护。若严格套衣字架，襄会取衣—廿，结构正确但记忆反直观；完整襄根使襄取襄—襄、镶钅—襄、壤土—襄。

字架已经正式接入 `build_analysis.py` 的拆分序列后处理，并由 `candidate_root_check.py` 共用，候选与正式表不再双轨。构建器从 chai 顶层结构自动识别同构字，以显式样例反推外框前后缀、保留中间件的当前根序列；完整成根者优先于字架。`audit_rules.py` 新增字架审计。当前审计：衣10、行9、辡4、玨2均已接管，襄显示“整字根优先”。正确基线复核襄由衣—廿变襄—襄，三码、全码新增均0。

囊／襄方案最终重构：襄不再独占主根，而与襄省一起挂衣；襄省是 chai 的囊下（襄去亠），项目自定义别名指向 PUA E83C。衣／衣省与襄／襄省形成对称家族，襄登记为概念字架（当前暂无向架内另塞部件的用户）。囊字头挂宀帽子家，故囊=宀—衣（帽子+襄省）、蠹=宀—虫、橐=宀—木。两项合并全部字音三码、全码新增均0，主根由160降回159。

`jb` 开始后井成为独立主根，覆盖进、讲、井、耕等；三码、全码新增均0。井挂艹／廾家会因 `jing` 音警、敬等同堆而使井丢失三码，故明确否决合并。

示成为独立主根，覆盖票、宗、祭、禁等65字／70字音；三码、全码新增均0。示挂礻虽无全码代价，但会在 `shi` 音被视78挤掉三码，故明确分家。

今成为主根并收令，两个亽／人字头家族合计改变49字／51字音；相对只立今没有任何额外三码、全码代价，并解除棽／椹零频碰撞。

祭字头与登字头（癶）挂夕，三码、全码新增均0；完整祭字头主要用于保护边界，登改夕—横、癸夕—大。侵字边明确不立，继续由末端又接管。

片成为主根并收丬、爿，修正将、装、奖、状等原先误落水键的问题；三码新增0、获救江，全码新增状44／奘0一对零频边界重码，接受。寝／寢中的丬／爿位于中段，码面仍为宀—又。

尺、尽均挂尸家；三种候选方案三码、全码均零增量，选不增加主根的 `尸:[户,尺,尽]`。尺／尽取尸—尸，迟尸—辶，烬火—尸。

贤字头挂览字头，不挂裸竖。裸竖案三码／全码均零代价，但主人更重视双竖右带部件的具体形体关系；接受 `jian` 音监33挤掉坚26三码，全码新增0。

留字头、餐字头挂夕。留零代价；餐按词组总频会挤掉残三码，但主人判断残的单字直打倾向更高，因此新增正式三码优先规则 `cj/夕=残`：残三、餐四。此规则只改指定首根堆，不污染二简或其他读音；音节表用★标记。

`di` 后续仍需讨论：帝／帝字头、啇，以及其余逐字是否已有直观首末路径。

剩余音节顺序：

`di、xl、jb、hv、qm、vf、yr、yc、xu、yk、yb、ge、bo、gu`

## 8. 协作方式

- 主人负责最终字形直觉和裁决；助手负责查全体数据、验证碰撞、执行落盘与审计。
- 一个候选一个候选来。可以一次列若干待审字，但不要替主人连续作决定。
- 先列数据，再解释判断；不要只给结论。
- 用户提出的具体部件边界优先认真验证，不要用 chai 的默认拆法反驳人眼结构。
- 语气可以轻松，但数据与“已决定／待决定”的状态必须严格区分。
- 亚作为完整变体挂业无横：无新增三码／全码风险，晋改取业无横—日，并救回恶、焉三码。
- jb 收官：堇维持廿+口+龶，不立根；整体立根无三码或全码收益。
- hv：惠与惠字头均不立；首横末心直观，惠在穗、蕙等末端仍由心接管。
- hv：回维持囗+口，不立根。
- hv：汇维持氵+匚，不立根。
- hv：灰不立；虽可无副作用救回卉三码，但收益仅一个低频字，不值得增加主根。
- hv：军维持冖+车，不立根；辉、挥、晖均由车直观收尾。
- hv：彗、慧均不立；分别由丰—争字底、丰—心接管。
- hv：毁与毁字底不立；维持臼+工+殳，取白—殳。
- hv：徽、徽字心不立，也不与微字心归并；徽／微均取彳—又，中间差异不可见。
- hv 收官：溃、卉、悔、荟、蕙、绘、烩、贿、喙、讳全部维持现拆，不新增根。
- qm：欠不立；虽覆盖55个首末用户，但全部性能指标为0变化，末端由人承担同样功能。
- qm：龺不立也不挂十；首十非常直观，维持十+日+十展开。
- qm 收官：其余常用字均由现成根接管，本音节不新增根。
- vf：辰不立；震、振、晨等继续由定字底收尾，全局立根无性能变化。
- vf：冘／枕字边不立；维持冖+儿，沈、枕、耽等由儿收尾。
- vf：瓦独立成根；主频61、14个部件用户，解决瓶／瓷／甄等末点反直观问题，零重码代价。
- vf 收官：瓦之外的余项全部由现成根接管，不再新增根。
- yk：新增赢字架主根，接管赢／嬴／羸／蠃／臝并传播瀛；赢改架—见、瀛改氵—女。
- yk 收官：除赢字架外，其余常用字均维持现拆。
- yr：原不立；维持厂+白+小，原／源／愿首末均直观。
- yr：袁不立；袁／猿／辕均由衣省直观收尾。
- yr：爰不立；暖、缓、援、媛等继续由又收尾。
- yr 收官：余项全部维持现拆，不新增根。
- pu：发现多音字总频作弊，指定pu／亻堆仆取三码、堡走全码；堡的高总频主要属于bao。
- pu 收官：不新增根，仅保留仆三、堡四人工简码规则。
- xn 收官：全音节由现成根接管，不新增根。
- xu：疋挂定字底，不增主根；救回弹三码，楚／疑／旋／凝等摆脱末点，零新增碰撞。
- xu 审计发现既有戊家实现漏项：补戌=[戊,横]、戍=[戊,点]，修正戌由广—点为戈—横。
- xu 收官：除疋挂定字底与戌／戍边界补漏外，其余维持现拆。
- yc：兆不立根，固定拆冫+冫+儿并传播全家；接受涛、沼失去三码，换取末儿直观与零新增主根。
- yc 收官：除兆家拆分调整外，其余不新增根。
- ge：鬲独立成根，接管融／隔等，解决内部末竖反直观问题；全局零碰撞代价。
- ge 收官：除鬲外，其余维持现拆。
- gu：骨独立成根，接管骨／滑／髓等；解决骨字头落裸竖，零性能代价。
- gu：完整爪、瓜挂爫家，不增主根；覆盖抓／爬／狐／孤／瓣等，零性能代价。
- gu 收官：新增骨、瓜挂爫，其余维持现拆。
- lk：齿不立；齿上止直观，龄取止—今，立根无性能收益。
- 插查乐：独立成根，覆盖le／yue及烁栎砾族；零新增风险并救回岳三码。
- lk 收官：令／今家与夌家均由现成根接管，不新增根。
- mu：恭字底⺗挂忄家；挂小家性能相同，但按本义选心，正式根表保持单宿主。
- 插查丘：独立成根，零性能代价；岳、邱、乒、乓等接管，兵边界另审。
- 丘落地后补兵边界：固定兵=[丘,八]；宾／滨等中间丘不可见，不逐字覆写。
- mu 收官：恭字底挂心后余项全部由现成根接管。
- mw 收官：全音节由现成根接管，不新增根。
- tj：单独立成根，接管弹／禅／蝉等多音字族；零性能代价，弹三码保持。
- tj 收官：除单外其余维持现拆。主根达到170，后续进入严格控根模式，优先挂靠与人工拆分。
- yb：印与印字旁挂卵家，不增主根；零新增碰撞并救回yb读音的殷三码。
- yb：弓挂折家，不增主根，覆盖张／强／引／弹等大族；正式码面仍同属折键，尹未获救。候选脚本曾把笔画5误作独立宿主而虚报收益，现已修正宿主归一化。
- yb：固定尹=[争字底,撇]并传播君／郡／群；不增根、零风险，真正救回尹三码。
- yb：壬挂土／士家，不增主根；淫改氵—土，并接管任／廷／丢等，零性能变化。yb 收官。
- bj 收官：全音节由片、舟、手家及玨／辡字架等现有根接管，不新增根。
- fj：凡挂几家，不增主根；接管筑／帆／梵／巩等，零性能变化。
- fj 插查食／飠挂饣：否决；会使饰失去三码且无全码收益，保持食=人+艮、飠不收。
- fj 收官：凡挂几，其余维持现拆；不新增主根。
- hj：函挂匚案否决；仅函本字换首根且性能零变化，维持折—匚。
- hj 收官：全音节维持现拆，不新增根。
- jx 收官：不新增根。至此前50个高密度音节全部过堂，主根170、附属/锚定220，下一阶段转全局漏根审计。
- 全局漏根审计首案：长独立成根，主根增至171；覆盖张／涨／账／帐／胀，全部读音零新增碰撞。
- 漏根审计：永独立成根，主根172；挂水案因伤涌三码并撞永／泳而否决，独立案零风险。
