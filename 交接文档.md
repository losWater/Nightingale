# 夜莺码工程交接文档（2026-08-19，v0.4 字根定稿版）

> 给下一个会话的 Claude（和主人自己）。读完本文 + `work/加根规则.md` 即可无缝接手。

## 一、工程现状一句话

夜莺码 = 音形方案（小鹤双拼 + 首末根形码两位，字根决不落 p 键）。当前 **v0.4 字根定稿版**：166 主根 + 71 附属形，加权三码率 95.6%+，键长 3.142，前500主码零重码，选重负担 0.065%（简单鹤9.3 为 0.711%）。布局层与字根层均已冻结，处于**实战扫雷维护期**：主人在练单软件过高频字，问题记入 `releases/v0.4/实战练字问题.txt`（分"必须改/改了最好/无关紧要"三档），攒批过堂。

## 二、万能工具箱（新会话第一件事：先用它，别手写脚本）

```
python scripts/nightingale.py <命令>
```

| 命令 | 用途 |
|---|---|
| `chai 字...` | 查拆分/简码/全部码及各码位排位（含词层） |
| `has 部件 [head/tail/any]` | 查含某部件的字（找家族用） |
| `slot 码...` | 查码位占用（纯单+总表双层） |
| `word 词` | 查词的落位 |
| `free 前缀` | 列空码位（找简码空位/御旨选址） |
| `pua` | 私用区(PUA)部件总览：标签/键位/就业/例字 |
| `dup [N]` | 主码口径重码报告 |
| `charword 字` | 字词冲突体检（保护字测试） |
| `addcode/delcode/setmain 字 码` | 加副码/删幽灵码/切主码 |
| `decree / decree-extra 字 码` | 御旨简码（写入 work/御旨.json；黜免直接编辑其 DENY 段） |
| `alt 字 根序列` | 多拆登记（查码页显示备拆） |
| `replace-split 根 模式... [--where] [--include/--exclude 字集]` | 批量改拆分（三份数据同步） |
| `sync` | 拆分↔码表对账补码 |
| `rebuild` | 全链重生成（八个生成器一键跑，含 Rime） |
| `census` | 字根就业普查（防幽灵根） |

## 三、数据流（谁生成谁）

```
真源: work/v04_final.tsv(.splits.tsv)  拆分数据（元素序列/显示序列）
      work/optimize/elements_v04.yaml  评测用首末简式
      work/readings.json               字→[[频,码]...] 主码在首位!
      releases/v0.4/夜莺码v0.4键位布局.yaml  根→键映射（附属={element:宿主}）
      work/御旨.json / work/多拆登记.tsv / 根目录 symbo.txt 补码.txt 备用码.txt
      work/四字词精选.txt(44315) / 夜莺码_词库17万瘦身版.txt / 扩展字集_打字版&全量.tsv

生成链（rebuild 一键全跑）:
  make_release.py → 纯单版 + 搜狗自定义短语（简码竞争/护指加成/副读音补位+竞争/御旨层在此）
  make_liuwei.py  → 留位版搜狗短语（搜狗部署版，10万上限，现81k）
  make_words.py   → 字词总表（查询权威，无上限）
  make_rime.py    → releases/v0.4/rime/ yeying.schema.yaml + yeying.dict.yaml（Mac 鼠须管用，sort:original 保持总表顺序）
  gen_tools.py    → 查码.html + 字根练习.txt + 练习器
  draw_chart.py   → 字根图.png
  gen_root_table.py → 字根总表.md
  root_census.py  → 体检（必跑）
```

改动纪律：**改拆分 → 改yaml(若动根) → sync → rebuild → census**，一步不能省。

## 四、核心规则速查（全文见 work/加根规则.md）

1. **原位立根**：部件首笔画所在键=默认落键，中间笔画对首末取根透明。双锚部件（首尾同键：民o/黑v/毛o）零元购，见一个抓一个。
2. **头尾不对称**：首位部件优先收；尾位仅当剩余强完形才立根；无名碎片挂形近大根（𠃓→勿、𧘇→衣）；双锚各安的双面部件不立根（艮案）。
3. **键内自洽**：根的记忆成本≈与同键锚点的推导距离；完形与拆解同键时立整体根（真=直八同b）。
4. **双码三分法**：一字多拆→双码并存+多拆登记；根搬家→旧码必须清除（幽灵码零容忍）；错拆→废。
5. **主码保位**：已持简码的字旧码保主位；切主码=换简码竞争前缀（化石简码退休术：母muo→mut、弓gso→gsg）。
6. **层级铁律**：扩展字永远队尾无脑让位；备胎(有简码字全码)排无简码字后；占用判定必须 (tier,频率) 排序。
7. **字词口径**：占用以字词总表为准；字vs词=保护字测试(独立频≥code_wmax)，源自三指标法（字常用度/独立性/冲突词频）；**词压字门槛1万**（2026-08-20：词频<1万的词无权夺无简码字首选，香樟压项案）。
8. **简码层**：一简26字母/二简/三简，护指加成×2只在三简层（末码iklo，2026-08-20由×3调降：四次偷袭大户全被否决）——**竞争裁决时别忘算加成**。副读音空位自动补、便利户≤5万+3倍频差自动夺位、>5万人工逐案。副读音简码不触发让全。
9. **改动前先摆数据给主人看，裁决权在主人**——尤其换防/夺位类。主人有极强的数据直觉（mut案/由yz案都是主人纠出来的）。

## 五、已知坑（血泪清单）

- **项目已是 git 仓库（2026-08-21 起，分支 main）**：改完一批记得 commit。`repos/`（hanzi-chai 克隆）被 .gitignore 排除——rebuild 里 make_liuwei/make_words/charword 要读 `repos/webchai/packages/hanzi-chai/src/data/dictionary.txt`，新机器先 `git clone https://github.com/hanzi-chai/hanzi-chai.github.io repos/webchai`。Mac 只需 releases/v0.4/rime/ 两个 yaml。
- `.gitattributes` 强制 LF；搜狗短语表是 UTF-16 按二进制处理，别用文本工具 diff 它。

- **Git Bash heredoc 吃反斜杠和 PUA 字符**：往脚本注入含 `\uXXXX`/`\n` 字面量或 PUA 字符时，用 Write/Edit 工具或 `chr(0xE902)`，绝不用 heredoc 内嵌。E900学字头/E901党字头/E902官腹曾因此丢失。
- **Python 打开文件用 `D:/...` 路径**（`/d/...` 是 bash 记法，Python 不认）。
- **console 是 GBK**：python 脚本顶部 `PYTHONIOENCODING=utf-8` 环境变量或 TextIOWrapper。
- **"前6000全码重"指标在双码架构下会虚高**（副码落占用位是设计允许），对外报数用 `dup` 的主码口径。
- **v031_final/elements 是首末简式**（同/网等少数多根字除外），splits.tsv 才是全序列显示。
- **留位版≠总表**：留位版是搜狗特化（有预算），总表是权威全量。四字词只从四字词精选进留位版。
- 混合码表/单字全码表已改由数据直接重生成（rebuild 内不含，如需参照 v0.4 会话记录逻辑），日常可忽略。

## 六、当前挂账（未完成事项）

1. **实战扫雷进行中**：主人明日过前500高频字，问题进 实战练字问题.txt（处理完在原条目下标 ✅+摘要）
2. 常用字扫雷 L2(374字)/L1(786字) 层未过（清单 work/常用字查漏清单.tsv）；候选池：㐄舛底(降舞舜麟)、丩
3. 不直观字集/必拆字练习表沿用旧版未重算（大批坑字已被新根消解）
4. 灰色区161字（work/独立率复核清单.tsv）+ 痛点半实213组
5. 新增根卡组 releases/v0.4/夜莺码v0.4新增根练习.txt（28张左右，随改动维护：加根加卡/退役删卡/迁键改键）
6. 对外资料包：字根图/新根速览/加根方法论/对标简单鹤报告（均在 releases/v0.4/，改动后记得更新）

## 七、记忆文件索引

长期记忆在 `C:\Users\asus\.claude\projects\D--nightingale\memory\`：维护期路线图、改根重码第一、双码三分法、三码率指标、头尾不对称+键内自洽、练习器进度机制（现已弃用迁移，改独立卡组）。版本流水在 `releases/版本说明.md`。

## 八、主人的工作风格（重要）

- 提案常以"想想/看看"开头 → 先给**数据全貌**，等裁决，不要抢跑（有过教训）
- 拆分描述可能有笔误 → 换防/交换类操作先复述确认方向
- 喜欢：账目表格、连锁推演（谁继任空位）、一句话记忆钩（"出随山走"）、判例命名
- 结案格式：实战问题.txt 原条目下加"✅已修(日期)：摘要"；版本说明.md 加一行；涉及根变动时更新卡组和速览表
