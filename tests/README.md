# 自动校验

当前测试覆盖0.8基础生成器、简码审计、字词交叉碰撞、随机布局约束、读音频率和最终保护运行器。

运行：

```powershell
python -m unittest discover -s tests -q
```

目前部分测试仍直接加载`夜莺B/scripts/`中的通用实现。这些文件虽然位于历史阶段目录，但在迁入`pipelines/`并完成兼容验证前仍属于受测试保护的活跃代码，不能随历史资料一起移动。
